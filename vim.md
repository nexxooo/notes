# 📑 Vim & LazyVim – Fiche Raccourcis

| Action                      | Vim classique             | LazyVim (leader = `<Space>`) |
|-----------------------------|---------------------------|-------------------------------|
| **Modes**                   | `i` insérer / `Esc` normal | idem                         |
| **Déplacements**            | `h` ← / `l` → / `0` début / `$` fin | idem             |
| Mot suivant / précédent     | `w` / `b`                 | idem                         |
| Début / fin fichier         | `gg` / `G`                | idem                         |
| **Copie / Coller**          | `yy` copier ligne / `yw` copier mot | idem             |
|                             | `p` coller après / `P` coller avant | idem             |
| **Suppression**             | `x` caractère / `dd` ligne | idem                         |
| **Annuler / Refaire**       | `u` / `Ctrl+r`            | idem                         |
| **Recherche texte**         | `/mot` → `n` / `N`        | idem                         |
| **Sauvegarde / quitter**    | `:w` / `:q` / `:wq` / `:q!` | idem                       |
| **Explorateur fichiers**    | —                         | `<Space> e`                  |
| **Ouvrir fichier**          | `:e fichier`              | `<Space> f f`                |
| **Buffers (onglets)**       | `:bnext` / `:bprev`       | `<Space> b b` / `<Space> b d`|
| **Recherche projet**        | —                         | `<Space> f g` (grep)         |
| **Git**                     | `:G` (si fugitive)        | `<Space> g g` (LazyGit)      |
| **Aller à la définition**   | `gd`                      | `gd`                         |
| **Renommer symbole**        | —                         | `<Space> c r`                |
| **Code actions**            | —                         | `<Space> c a`                |
